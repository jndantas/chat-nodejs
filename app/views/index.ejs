<!DOCTYPE HTML>
<html lang="pt-br">
	<head>
		<meta charset="UTF-8">

		<title>Multiroom Chat</title>
		
		<!-- bootstrap - link cdn -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
	
		<!-- estilo -->
		<link href="css/style.css" rel="stylesheet">
	</head>

	<body>

		<div class="container">
            <div class="row justify-content-md-center vertical-offset-80">
                <div class="col-md-4 col-md-offset-4">
                    <div class="card card-default">
                    	<div class="card-heading">                                
                            <div class="row-fluid user-row">
                                <img src="images/multiroom_chat_2.png" class="img-responsive" alt="Multiroom Chat"/>
                            </div>
                        </div>
                        <div class="card-body">
                            <form accept-charset="UTF-8" role="form" class="form-signin" action="/chat" method="post">
                                <fieldset>
                                    <label class="card-login">
                                        <div class="login_result"></div>
                                    </label>
                                    <input class="form-control" placeholder="Nome ou apelido" id="apelido" name="apelido" type="text">
                                    <br></br>
                                    <div class="d-grid gap-2">
                                        <input class="btn btn-lg btn-success btn-block" type="submit" id="entrar" value="Entrar »">
                                      </div>
                                
                                </fieldset>
                            </form>
                        </div>
                    </div>
                    <div class="pt-3">
                        <% if(validations.length > 0) {%>
                            <div class="alert alert-danger d-flex align-items-center" role="alert">
                                <h4 class="alert-heading"><svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Danger:"><use xlink:href="#exclamation-triangle-fill"/></svg>
                                    Atenção!</h4>
                                <hr>
                               <ul>
                                    <% for(let i=0; i < validations.length; i++) { %>
                                        <li><%= validations[i].msg %></li>
                                    <% } %>
                               </ul>
                              </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
		
	</body>
</html>